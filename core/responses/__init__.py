"""OpenAI Responses API primitives for ``codexproxy``.

This package provides the shared, provider-agnostic building blocks used by
``api/responses_routes.py`` and any provider that emits Responses-shaped
SSE. It mirrors the structure of :mod:`core.anthropic` so contributors
familiar with the Anthropic wire surface can navigate both packages.
"""
